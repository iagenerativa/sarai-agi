# Directorio para modelos GGUF, checkpoints y artefactos ML

Los modelos grandes no se versionan en Git. Usar `scripts/download_models.sh` para obtenerlos.

## Estructura sugerida

```
models/
├── gguf/              # Modelos cuantizados GGUF
├── embeddings/        # Modelos de embeddings
├── checkpoints/       # Checkpoints de entrenamiento
└── cache/            # Cache de modelos temporales
```
